package codegen

import(
    _"text/template"
    _"io"
)

func ParseCodeTemplate(tpl string,data interface{}) (parsed string,err error){
    /*
    var rw io.ReadWriter
    tmpl, err := template.New("dummy").Parse(tpl)
    if err != nil { return  }
    err = tmpl.Execute(rw, data)
    if err != nil { return  }
    */
    return

}
